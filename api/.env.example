# Local test DB (SQLite) - set to 1 to use local.db instead of Oracle (chakra bypass)
# USE_LOCAL_DB=1

# Oracle DB
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_CONNECT_STRING=localhost:1521/ORCL

# Thick mode: 64-bit Oracle Instant Client only (Node.js 64bit requires 64bit Oracle Client)
# Download: https://www.oracle.com/database/technologies/instant-client/winx64-64-downloads.html
# Extract -> set path to folder containing oci.dll
# ORACLE_CLIENT_LIB_DIR=C:\oracle\instantclient_21_13

# Option: table/column names (if different from default)
# DB_TABLE=USERS
# DB_USER_ID_COL=USER_ID
# DB_PASSWORD_COL=PASSWORD
